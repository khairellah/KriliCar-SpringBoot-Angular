// Reflète Backend/src/main/java/com/kriliCar/dtos/BrandDTO.java (écriture)
// `code` est volontairement absent : toujours généré/ignoré côté backend
// (@Mapping(target = "code", ignore = true) dans BrandMapper).
export interface BrandRequest {
  name: string;
}